/** Shared type definitions for test data across all EcoSet Config test suites. */

export interface ProjectSetup {
  projectName: string;
  switchboardName: string;
}

export interface ContactDetails {
  email: string;
  phone: string;
}
